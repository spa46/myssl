#!/bin/bash
# test_04_revoked - Revoked Certificate with CRL Check
# Expected: error code 23 (The certificate has been revoked)

openssl s_client -connect localhost:4443 \
  -servername revoked.example.com \
  -CAfile ca-chain.pem \
  -CApath crl \
  -crl_check_all \
  -brief \
  <<< "Q" 2>&1

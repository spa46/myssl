#!/bin/bash
# test_01_valid - Valid Certificate
# Expected: error code 0 (Success)

openssl s_client -connect localhost:4443 \
  -servername valid.example.com \
  -CAfile ca-chain.pem \
  -verify_return_error \
  -brief \
  <<< "Q" 2>&1


#!/bin/bash
# test_03_expired - Expired Certificate
# Expected: error code 10 (Certificate has expired)

openssl s_client -connect localhost:4443 \
  -servername expired.example.com \
  -CAfile ca-chain.pem \
  -verify_return_error \
  -brief \
  <<< "Q" 2>&1

